/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef, useState } from 'react';
import { useGLTF } from '@react-three/drei';
import { useFrame, useThree } from '@react-three/fiber';
import { useTheme } from 'next-themes';

export function DarkModeToggle(props: JSX.IntrinsicElements['group']) {
  const { width, height } = useThree((state) => state.viewport);
  //@ts-ignore
  const { nodes: nodesSun, materials: materialsSun } = useGLTF('/sun.glb');
  //@ts-ignore
  const { nodes: nodesMoon, materials: materialsMoon } = useGLTF('/moon.glb');
  const ref = useRef<any>();
  const [hovered, setHovered] = useState(false);
  const { theme, setTheme } = useTheme();

  useEffect(() => {
    document.body.style.cursor = hovered ? 'pointer' : 'auto';
  }, [hovered]);

  useFrame(() => {
    if (hovered) {
      ref.current.rotation.y += 0.015;
    } else {
      ref.current.rotation.y += 0.005;
    }
  });

  return (
    <group
      ref={ref}
      {...props}
      position={[width / 2 - 1, height / 2 - 1, 0]}
      dispose={null}
      onPointerOver={() => setHovered(true)}
      onPointerOut={() => setHovered(false)}
      onClick={() => {
        setTheme(theme === 'light' ? 'dark' : 'light');
      }}
    >
      <mesh
        castShadow
        receiveShadow
        geometry={nodesMoon.Cube008.geometry}
        material={materialsMoon['Default OBJ.005']}
        visible={theme === 'dark'}
      />

      <mesh
        castShadow
        receiveShadow
        geometry={nodesSun.Cube001.geometry}
        material={materialsSun.None}
        rotation={[Math.PI / 2, 0, 0]}
        scale={1000}
        visible={theme === 'light'}
      />
    </group>
  );
}

useGLTF.preload('/moon.glb');
